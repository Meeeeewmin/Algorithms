//
#include <iostream>



/*
#include <iostream>
#include <algorithm>

using namespace std;

bool compare(int a, int b){
    return a > b;
}

int main_code() { //int argc, const char * argv[]
    // insert code here...
    int a[10] = {9,3,5,4,1,10,8,6,7,2};
    sort(a, a+sizeof(a)/sizeof(*a), compare);
    for(int i = 0; i<10; i++){
        std::cout << a[i] << ' ';
    }
    std::cout << "Hello, World!\n";
    return 0;
}

class Student{
public:
    string name;
    int score;
    
    Student(string name, int score){
        this->name = name;
        this->score = score;
    }
    
    bool operator < (const Student& student)
    const{
        return this->score < student.score;
    }
};


int main(){
    Student students[] = {
        Student("나동빈", 90),
        Student("이상욱", 93),
        Student("박한울", 97),
        Student("강종구", 87),
        Student("이태일", 92)
    };
    sort(students, students+5);
    for(int i=0; i<5; i++)
        std::cout << students[i].name << ' ';
    
    main_code();
    
}
*/
